{
  "name": "root",
  "private": true,
  "workspaces": [
    "games/*"
  ],
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap --hoist --force-local",
    "build": "lerna run --stream build",
    "test": "lerna run --stream test"
  }
}
